'use strict';

const User = require('./user');
const UserInformation = require('./user-information');
const Address = require('./address');

module.exports = {
    User,
    UserInformation,
    Address
};
